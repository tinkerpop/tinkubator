"http://xmpp.org/extensions/inbox/lop.html#sect-id2257047":http://xmpp.org/extensions/inbox/lop.html#sect-id2257047

  * label="supported virtual machine species" var="vm_species" type="list-single"
  * label="the IP address of the device hosting the farm" var="ip_address" type="list-single"
  * label="exercise a permission" var="exercise_permission" type="boolean"
  * label="create a Java class loader" var="create_class_loader" type="boolean"
  * label="modify a Java thread or thread group" var="modify_thread" type="boolean"
  * label="exit the farm process" var="shutdown_farm" type="boolean"
  * label="execute a program" var="execute_program" type="list-multi"
  * label="create a file system link" var="create_file_link" type="list-multi"
  * label="read from a file" var="read_file" type="list-multi"
  * label="write to a file" var="write_file" type="list-multi"
  * label="delete a file" var="delete_file" type="list-multi"
  * label="open a socket connection" var="open_connection" type="boolean"
  * label="wait for a connection request" var="listen_for_connection" type="boolean"
  * label="accept a socket connection" var="accept_connection" type="boolean"
  * label="use IP multicast" var="perform_multicast" type="boolean"
  * label="access or modify system properties" var="access_properties" type="boolean"
  * label="access or modify a system property" var="access_property" type="boolean"
  * label="initiate a print job request" var="access_print_job" type="boolean"
  * label="access the system clipboard" var="access_system_clipboard" type="boolean"
  * label="access a Java package" var="access_package" type="boolean"
  * label="define classes in a package" var="define_class" type="boolean"
  * label="set a socket factory" var="set_socket_factory" type="boolean"
  * label="access Java members" var="access_member" type="boolean"
  * label="exercise a named permission" var="exercise_named_permission" type="boolean"
  * label="version of the Java language" var="java_version" type="text-single"
  * label="name of the Java virtual machine" var="java_vm_name" type="text-single"
  * label="version of the Java virtual machine" var="java_vm_version" type="text-single"
  * label="operating system architecture" var="os_arch" type="text-single"
  * label="operating system name" var="os_name" type="text-single"
  * label="operating system version" var="os_version" type="text-single"
  * label="the xsd:dateTime at which this farm was started" var="farm_start_time" type="text-single"
  * label="whether a password is required to spawn additional virtual machines on this farm" var="farm_password_required" type="boolean"
  * label="the number of concurrent virtual machines which this farm can support before it rejects a request to spawn a new virtual machine" var="max_concurrent_vms" type="text-single"
  * label="the number of jobs which a virtual machine can hold in its queue before it rejects requests to submit additional jobs" var="job_queue_capacity" type="text-single"
  * label="the number of milliseconds for which a job may execute before it is aborted by this farm" var="job_timeout" type="text-single"
  * label="the number of milliseconds for which a virtual machine may exist before it is terminated by this farm" var="vm_time_to_live" type="text-single"